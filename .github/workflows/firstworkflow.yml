name : firstworkflow

on:
    push:
    
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - run : echo "hello welcome to github actions"
            - run : mvn clean install