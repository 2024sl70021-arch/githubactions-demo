name: firstworkflow

on:
    workflow_dispatch:


jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - run: echo "hello everyone"
            - run: echo " welcome to github actions"



